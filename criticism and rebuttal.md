# 프로젝트에 관한 비판과 반박

> 개정 이력
> - 2026-09-08 초안
> - 2026-09-08 개정: Trivy SG 우회 실험([`experiments/trivy-sg-probe/`](experiments/trivy-sg-probe/)) 결과를 반영해 비판 2의 답변을 전면 수정. 비판 5 신설.

---

## 비판 1. "기업에 필요 없다, 사람 더 쓰면 되지 않나"

이 논리대로면 CI/CD도, 린터도, 관제 자동화도 전부 필요 없다. 실제 답은 **리뷰 병목**이다. 조직이 커지면 인프라 PR은 늘어나는데 보안 검토 인력은 그만큼 늘지 않는다. 보안 인력이 비싸고 구하기 어렵다는 것은 A안이 배경으로 든 근거와 같으므로, 이 비판은 A안에도 똑같이 적용된다. 즉 주제 선택의 근거로는 약하다.

**다만 우리 프로젝트가 반박할 수 있는 지점은 따로 있다.** "사람을 더 쓰면 된다"의 반대편, 즉 "그럼 자동화하면 되지"에 대한 답이 우리에게 있다.

> TerraProbe 연구(arXiv:2606.26590)는 실제 GitHub Terraform 코드에서 LLM 패치의 57.1~71.4%가 스캐너·`validate`·`plan`을 모두 통과하면서 실제 권한을 그대로 유지했다고 보고했다. 우리도 Trivy 0.74.0에서 같은 성격의 우회를 2종 확인했다(01-cidr-split, 06-prefix-list). **검토 없이 자동화하면 이런 패치가 그대로 통과한다.**

즉 우리 답은 "사람이 부족하니 AI를 쓰자"가 아니라 **"자동화하려면 검증이 먼저 필요하고, 기존 스캐너만으로는 부족하다는 것을 실측했다"** 이다.

근거로 사용하는 수치는 직접 측정한 것과 논문에 명시된 것으로 한정한다. "클라우드 사고의 최다 원인이 설정 오류"처럼 널리 인용되지만 원문을 확인하지 않은 통계는 사용하지 않는다.

---

## 비판 2. "브랜치를 나눌 거면 승인이 왜 필요하냐"

정확한 지적이고, 설계의 진짜 구멍이었다. 모든 등급이 결국 사람 승인으로 끝나면 High/Medium/Low는 PR 라벨 차이일 뿐이고, "자율성을 조절한다"는 말이 허울이 된다.

### 초안의 답과 그것이 틀린 이유

초안에서는 이렇게 답했다.

> "High는 자동 머지 후 자동 apply, **재스캔과 헬스체크가 실패하면 자동 revert**"

**이 답은 우리 자신의 실험으로 반박됐다.** 01-cidr-split 케이스를 이 파이프라인에 넣으면 이렇게 된다.

| 단계 | 결과 |
|---|---|
| Trivy 재스캔 | 0 FAIL — 통과 |
| 헬스체크 | 허용 IP에서 SSH 정상 — 통과 |
| 자동 revert | **발동하지 않음** |
| 실제 AWS 상태 | **22번 포트가 전 인터넷에 개방** |

안전망으로 제안한 두 가지가, 정확히 기만적 패치가 통과하는 두 가지다. 초안은 실험 전에 작성했으므로 당시로서는 합리적이었으나, **지금 이대로 구현하면 위험한 설계가 된다.**

### 개정된 답 — 운영 경로와 실험 경로를 분리한다

```
운영 경로 (파이프라인 정의)
  검증 스택 V1~V6 통과 → PR 생성 → 사람 승인 → apply → V7·V8

실험 경로 (측정 전용, AWS 샌드박스 한정)
  검증 스택 V1~V6 통과 → 자동 병합 → 자동 apply
  → V6 Intent Oracle / V7 AWS 상태 실측 실패 시 자동 revert
  → 사람 개입 없는 순수 자동 처리 시간을 측정
```

**핵심은 revert 트리거다.** 초안이 제안한 "재스캔 + 헬스체크"는 01번을 잡지 못하지만, **V6 Intent Oracle과 V7 실측은 잡는다.** 이 차이가 실험으로 증명된 것이 이번 개정의 근거다.

실험 경로가 필요한 이유는 하나 더 있다. 우리 평가 지표에 **"자동 처리 시간과 사람 승인 대기 시간을 분리 측정"** 이 있는데, 사람 없이 끝까지 가는 경로를 한 번은 돌려봐야 그 숫자를 만들 수 있다.

### 그래서 이 비판에 대한 답

> "완전 자동 배포를 실제로 돌려서 측정합니다. 다만 안전망이 재스캔이 아니라 Intent Oracle과 AWS 실측입니다. 재스캔만으로는 못 잡는 패치가 존재한다는 것을 직접 측정했기 때문입니다. 운영 파이프라인 정의에서 승인을 남긴 이유도 같습니다."

### 명시할 것

- 실험 경로의 무인 `apply`는 **AWS 샌드박스 계정 한정**이며, 프로덕션에는 적용하지 않는다
- 실험 경로 실행 사실을 문서에서 숨기지 않는다
- 자동 revert가 실패한 사례가 나오면 그것도 결과로 기록한다

---

## 비판 3. "확신도를 AI에게 맡기는 게 맞냐"

**맡기지 않는다.** 구조는 이렇다.

1. LLM은 패치안과 함께 확신도 등급을 **제안**한다
2. Risk Rubric(사람이 사전에 정한 위험도 기준)이 **자율성 등급의 상한을 강제**한다
3. LLM은 등급을 **낮출 수는 있어도 상한 위로 올릴 수 없다**

즉 LLM의 신호는 **하향 방향으로만** 작동하므로 구조적으로 안전하다. 논문이 반박하는 것은 "LLM이 자기 등급을 올릴 수 있는 구조"인데, 우리 설계는 그것을 애초에 막는다.

### 초안에서 고칠 부분 — 지표 정의

초안은 "AI의 확신도 판단이 사람의 위험도 판단과 얼마나 일치하는가"를 지표로 삼겠다고 했다. 그런데 Scorer가 상한을 강제하므로, 상한에 걸린 케이스는 이미 강제로 맞춰진 것이다. **"일치율"이 무엇을 재는지 모호하다.**

정확히 재야 할 것은 둘이다.

| 지표 | 정의 |
|---|---|
| 상한 도달 분포 | LLM 제안 등급이 상한에 걸린 비율 / 스스로 낮춘 비율 |
| **하향 신호의 정보 가치** | **LLM이 스스로 낮춘 케이스가 실제로 문제 있는 패치였는가** |

두 번째가 핵심이다. **LLM을 등급 판정에 참여시키는 유일한 정당화가 "하향 신호에 정보 가치가 있다"는 것이므로, 그것을 직접 측정한다.** 정보 가치가 없다는 결과가 나오면 LLM의 등급 제안을 제거하면 되고, 그것도 유효한 결과다.

---

## 비판 4. "CIS Benchmark면 정해진 기준 아니냐, AI가 뭘 판단하냐"

네 비판 중 가장 날카롭다. **절반은 맞다.**

**탐지에는 AI가 기여하지 않는다.** Trivy가 결정론적으로 한다. 이를 "CIS 기반으로 AI가 판단"이라고 표현하면 틀린 말이다.

AI가 들어가는 곳은 **수정**이다. CIS는 "SSH를 0.0.0.0/0에 열지 마라"고 하지만 "어느 대역으로 좁혀라"는 말하지 않는다. "IAM 정책이 과다하다"고 하지만 "이 애플리케이션이 실제로 필요한 권한이 무엇인지"는 코드를 읽어야 안다. 이 부분에는 정해진 기준이 없다.

### 인정하고 설계에 반영한 것

초안에서 이미 인정했듯, **"퍼블릭 S3 차단"은 수정도 정해져 있다.** `block_public_acls = true` 한 줄이면 끝난다. 이런 유형은 Rule-based가 AI보다 낫고, 실험하면 baseline이 이길 것이다.

**그래서 필수 유형에서 S3를 제외했다.** 현재 README의 필수 유형은 보안그룹 CIDR과 IAM 최소 권한 두 가지이며, 둘 다 맥락 의존적이다. S3와 컨테이너 CVE는 선택 확장으로 내렸다.

### 실험이 이 주장에 근거를 더했다

"보안그룹 CIDR이 맥락 의존적"이라는 주장이 이제 실측으로 뒷받침된다. 01-cidr-split이 보여주는 것은 **"어디로 좁힐지에 정답이 없고, 잘못 좁히면(쪼개면) 스캐너도 잡지 못한다"** 는 것이다. 정답이 하나인 문제였다면 이런 우회가 성립하지 않는다.

### 연구 질문에 명시한다

숨기지 않고 이렇게 쓴다.

> **단순 유형은 Rule-based가 이기고, 맥락 의존 유형에서 AI가 이기는지가 관건이다.**

그래야 이 비판이 나왔을 때 "알고 있고, 그것이 실험 설계다"라고 답할 수 있다.

---

## 비판 5. "그러면 AI Agent는 왜 쓰는가"

검증 스택을 결정론적으로 만들수록 AI가 차지하는 자리가 줄어든다. 실제로 세어보면 이렇다.

| 단계 | 담당 | AI 기여 |
|---|---|---|
| 탐지 | Trivy | 없음 |
| Evidence Bundle | 스크립트 | 없음 |
| 검증 V1~V8 | 결정론적 코드 | 없음 (V6는 명시적으로 Trivy도 AI도 쓰지 않음) |
| 자율성 등급 | Risk Rubric | 없음 (LLM은 하향 제안만) |
| PR 생성 / revert | GitHub API, V6·V7 트리거 | 없음 |
| **패치 생성** | **LLM** | **여기 하나** |

**AI가 남는 자리는 패치 생성 하나다.** 이것을 숨기지 않는다. 그리고 비판 4에서 인정했듯 단순 유형에서는 그마저도 Rule-based가 이길 가능성이 높다.

### 그럼에도 AI가 필요한 이유

**① 검증기를 평가할 대상이 필요하다.**
Seeded 세트(01/06/08)는 우리가 손으로 만든 것이다. 그것만으로는 "V6가 알려진 패턴을 잡는다"까지만 말할 수 있다. **"실제 LLM 출력에서도 잡는다"** 를 말하려면 LLM이 패치를 생성해야 한다. 이것이 현재 미측정으로 남아 있는 항목이다.

**② 연구 질문 3번이 AI 없이는 성립하지 않는다.**
같은 보안그룹 설정 오류라도 형태가 다양하다. `cidr_blocks` 직접 지정, `dynamic` 블록 생성, 변수 경유, 별도 리소스(`aws_vpc_security_group_ingress_rule`) 등. Rule-based는 형태마다 패턴을 추가해야 하고, AI는 HCL을 읽고 대응할 수 있다. **변형 대응력의 차이를 재는 것이 연구 질문 3번이다.** 실험 케이스 02~05, 07이 그대로 변형 세트가 된다.

**③ IAM은 코드 컨텍스트가 필요하다.**
`Action: "s3:*", Resource: "*"` 를 최소 권한으로 좁히려면 애플리케이션이 실제로 어떤 작업을 하는지 알아야 한다. Rule-based로는 원리적으로 불가능하다. 다만 **AI가 이것을 잘할지는 미지수다.** 논문의 기만적 패치 10건 중 9건이 IAM이었으므로 실패 가능성이 높다. 실패도 결과다.

### 프로젝트 정체성의 위치 변화

| | 이전 | 현재 |
|---|---|---|
| 주인공 | AI Agent | 검증 스택 |
| AI의 위치 | 시스템의 핵심 도구 | **검증 대상** |
| "AI 왜 쓰냐"에 대한 답 | 방어하기 어려움 | 방어할 필요가 없음 |

> **"AI가 좋다고 주장하는 것이 아닙니다. AI 기반 IaC 자동 수리가 이미 연구·도입되고 있는데 57~71%가 기만적 패치라는 보고가 있고, 저희는 그것을 막는 장치를 만듭니다. AI는 저희 시스템의 도구가 아니라 검증 대상입니다."**

### 어떤 결과가 나와도 프로젝트는 성립한다

| 실험 결과 | 프로젝트 가치 |
|---|---|
| 단순 유형에서 Rule-based 승 | 예측대로. "AI를 어디 쓰면 안 되는지"에 대한 데이터 |
| 변형 유형에서 AI 승 | 연구 질문 3번에 긍정 답 |
| IAM에서 AI가 기만적 패치 생성 | V6가 잡아내는 실제 사례 확보. 가장 강한 결과 |
| 전 영역에서 AI가 무의미 | 그것도 결과. 검증 스택은 여전히 유효 |

**어느 쪽으로 나와도 검증 스택은 살아남는다.** 프로젝트 성패가 AI 성능에 종속되지 않는 구조다.

---

## 비판 6. "게이팅 기준이 확신도인가 위험도인가"

**맞다. 그래서 정정했다.**

게이트가 실제로 사용하는 판정 기준은 변경 리소스 종류, 영향 범위, IAM 관련 여부, blast radius, 롤백 난이도다. 이는 모두 **위험도(Risk)** 이지 확신도(Confidence)가 아니다. AI의 확신도는 등급을 낮추는 방향으로만 반영되는 부수 입력이다.

또한 운영 경로에서 사람 승인이 필수이므로 "자율 패치"라는 표현도 과장이다.

| | 이전 | 정정 |
|---|---|---|
| 제목 | 확신도 기반 클라우드 인프라 자율 보안 패치 파이프라인 | **AWS IaC 보안 패치 검증 파이프라인** |
| 게이팅 근거 | 확신도 | **위험도(Risk Rubric)** |
| 등급 의미 | Confidence 등급 | **자율성 등급** (위험도가 낮을수록 High) |

**표기 변경은 실측 결과에 맞춰 용어를 정확하게 정리한 것이다.** 게이팅의 실제 판정 기준이 무엇인지 확인한 뒤 그에 맞게 고쳤을 뿐, 방향이 바뀐 것이 아니다.

지도교수께 보고한 주제명은 "클라우드 인프라 기반 보안 패치 파이프라인"이며, 현재 제목은 그것을 구체화한 것이다.
