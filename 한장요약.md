# 한 장 요약

**AWS IaC 보안 패치 검증 파이프라인**
사이버보안과 졸업작품 · 3인 · 2026.09.08 ~ 12.04

---

## 무엇을 만드는가

Terraform으로 작성한 AWS 인프라의 보안 설정 오류를 탐지하고,
AI가 수정 코드를 만들면 **그 수정이 실제로 보안을 개선했는지 검증하는** 파이프라인.

---

## 왜 필요한가

스캐너를 통과했는데 실제로는 전혀 안 고쳐진 경우가 있다. 직접 만들어 확인했다.

```
  수정 전   cidr_blocks = ["0.0.0.0/0"]                  → Trivy 탐지  O
  수정 후   cidr_blocks = ["0.0.0.0/1", "128.0.0.0/1"]   → Trivy 탐지  X
```

두 대역의 합집합은 `0.0.0.0/0`과 **수학적으로 동일하다.**
22번 포트는 여전히 전 인터넷에 열려 있는데 스캐너만 조용해졌다.

Security Group 우회 후보 9종을 만들어 스캔했고, **2종이 이런 식으로 통과했다.**
(Trivy 0.74.0 / Terraform 1.16.1 · 검증 기록: `experiments/trivy-sg-probe/VERIFY.md`)

**이 현상은 이미 보고된 바 있다.** TerraProbe 연구(arXiv:2606.26590, 2026.06)는
LLM이 만든 Terraform 보안 패치의 **57~71%** 가 스캐너·`validate`·`plan`을 모두
통과하면서도 실제 권한은 그대로 유지했다고 보고했다. 모델 3종에서 차이가 없어
구조적 문제로 분석됐다. 다만 그 연구는 Checkov 기준이라, 우리가 사용할 Trivy에서도
같은 격차가 있는지 위와 같이 직접 측정했다.

**결론: "재스캔에서 오류가 사라졌다"를 성공 기준으로 쓸 수 없다.**

---

## 그래서 무엇을 만드는가

```
   Terraform 변경 → Trivy 탐지 → AI가 패치 생성
                                      ↓
                            ┌─── 검증 8계층 ───┐
                            │ 여기서 걸러낸다   │
                            └─────────┬────────┘
                                      ↓
                        위험도 판정 → PR → 사람 승인 → 배포
                                      ↓
                            실제 AWS 상태 확인
```

검증 계층 중 핵심은 **Intent Oracle**이다.
스캐너 결과를 쓰지 않고, 해당 포트로 들어오는 모든 출처의 CIDR 합집합을 직접 계산해
실제로 열려 있는지 판정한다. 위 우회 사례를 문자열 검사로는 잡을 수 없기 때문이다.

배포 후에는 `describe-security-groups`로 **실제 AWS 상태를 확인**한다.
TerraProbe는 배포를 수행하지 않아 `plan` 결과가 최종 증거였고, 기만적 패치 판정도
사람이 직접 했다. 우리는 샌드박스가 있어 실측이 가능하고, Security Group에 한해서는
CIDR 계산으로 **자동 판정**이 가능하다.

---

## AI는 무엇을 하는가

| 단계 | 담당 |
|---|---|
| 탐지 | Trivy |
| 검증 | 결정론적 코드 (AI 사용 안 함) |
| 자동화 등급 | 사전 정의된 위험도 기준표 |
| **패치 생성** | **AI (LLM)** |

**AI가 관여하는 곳은 패치 생성 하나뿐이다.**
AI가 등급을 제안할 수는 있으나 기준표가 정한 상한 위로 올릴 수 없고, 낮추는 것만 가능하다.

이 프로젝트는 AI의 우수함을 주장하지 않는다. **AI가 만든 결과를 검증하는 것이 주제다.**
규칙 기반(Rule-based) 방식과 비교 실험을 하며, **규칙 기반이 더 낫다는 결과도
유효한 결과로 인정하고 그대로 보고한다.**

---

## 범위

**필수** — 과다 개방 Security Group, IAM 과다 권한
**여유 시** — 퍼블릭 S3, 컨테이너 이미지 CVE
**제외** — EKS/Kubernetes, 추가 스캐너, 대시보드

S3를 필수에서 제외한 이유: 수정 방법이 한 줄로 정해져 있어 규칙 기반이 AI보다 낫다.
AI를 쓸 이유가 없는 유형이므로 비교 실험의 대상으로만 둔다.

---

## 일정

| 기간 | 내용 |
|---|---|
| 9/8 ~ 11/16 | 개발 (10주). **11/16 기능 동결** |
| 11/17 ~ 11/23 | 반복 실험 + 통합 테스트 |
| 11/24 ~ 12/2 | 결과 정리 · 발표자료 · 데모 녹화 · 리허설 |
| **12/3 ~ 12/4** | **발표** |

주요 점검 지점

- 5주차 — 탐지에서 PR 생성까지 자동 동작, 3회 재현
- **6주차 — Intent Oracle이 우회 사례를 잡고 정상 패치는 통과시킴**
- 8주차 — 검증 계층 전체 동작

---

## 아직 확인하지 못한 것

추측으로 채우지 않고 확인이 필요한 항목이다.

- 위 우회 사례가 **배포 후에도** 실제로 포트를 개방하는지 (`validate`는 문법만 검증)
- **AI가 저런 패턴을 실제로 생성하는지.** 이번 실험은 탐지기 능력 측정이며
  자연 발생률 측정이 아니다. TerraProbe와 주장의 층위가 다르므로 구분해 서술한다
- Trivy 룰 중 CIS AWS Benchmark와 직접 대응 가능한 항목의 범위

핵심 연구 질문(자율성 게이팅이 안전성과 효율의 균형을 개선하는가)은
**아직 검증되지 않았으며, 11월 비교 실험으로 답한다.**
현 시점에 확보한 것은 "왜 이 시스템이 필요한가"에 대한 근거다.

---

## 더 보시려면

| 파일 | 내용 |
|---|---|
| `README.md` | 전체 계획. 0절에 위 근거, 2절에 검증 계층 설계 |
| `experiments/trivy-sg-probe/VERIFY.md` | 위 실험의 검증 기록. 원본 JSON과 스크립트 포함 |
| `criticism and rebuttal.md` | 예상되는 지적과 답변 |
| `RoadMap.md` | 동작 시나리오와 용어집 |
