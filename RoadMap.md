#동작 흐름 (시나리오)

팀원이 AWS에 새 서비스를 올리려고 Terraform 코드를 작성합니다. 예를 들어 S3 버킷 하나와 EC2 인스턴스, 그리고 EC2용 보안 그룹을 정의했는데, 급하게 짜느라 보안 그룹의 SSH 포트(22번)를 0.0.0.0/0, 즉 전 세계에 열어놓았고 S3 버킷은 퍼블릭 접근 차단 설정을 빼먹었습니다. 이 코드를 GitHub에 PR로 올립니다.

PR이 올라오는 순간 GitHub Actions가 자동으로 실행됩니다. 파이프라인은 먼저 `trivy config`로 Terraform 코드를 훑어서 CIS AWS Benchmark 기준에 어긋나는 설정을 찾아냅니다. 여기서 "SSH 전체 개방(HIGH)"과 "S3 퍼블릭 접근 차단 누락(HIGH)" 두 건이 잡힙니다. 같은 PR에 Dockerfile이 있다면 `trivy image`로 컨테이너 이미지의 CVE도 함께 스캔합니다. 결과는 JSON으로 저장되어 다음 단계로 넘어갑니다.

AI Agent가 이 JSON을 받습니다. Agent는 탐지 항목마다 문제가 된 Terraform 코드 조각과 Trivy가 알려준 규칙 ID, 설명을 함께 읽고 수정 코드를 작성합니다. 보안 그룹은 CIDR을 사내 IP 대역으로 좁히고, S3에는 `aws_s3_bucket_public_access_block` 리소스를 추가하는 식입니다. 그리고 각 패치에 확신도를 매깁니다. S3 퍼블릭 차단 추가는 정답이 하나뿐이고 부작용이 없으니 High, 보안 그룹 CIDR 변경은 "어느 대역으로 좁혀야 하는지"를 Agent가 확실히 알 수 없으니 Medium입니다.

여기서 확신도 게이트가 작동합니다. High 패치는 Agent가 직접 커밋해서 원래 PR에 추가하고 자동 승인 대상으로 표시합니다. Medium 패치는 별도 커밋으로 올리되 "리뷰 필요" 라벨을 붙이고 근거를 PR 코멘트로 남깁니다. Low였다면 코드는 건드리지 않고 "이런 문제가 있고 이런 방향으로 고쳐야 한다"는 진단 코멘트만 남깁니다.

패치가 적용된 코드는 다시 `trivy config`를 통과해야 합니다. 앞서 잡힌 항목이 사라졌는지, 패치 때문에 새 문제가 생기지 않았는지 확인하는 재검증 단계입니다. 통과하면 `terraform plan`을 돌려 실제 AWS에 어떤 변경이 일어날지도 함께 출력합니다.

사람 개발자는 PR 화면에서 "S3 설정은 자동으로 고쳐졌고, 보안 그룹은 AI가 제안한 대역이 맞는지 확인해 달라"는 정리를 보게 됩니다. 확인 후 머지하면 `terraform apply`로 AWS에 배포되고, 컨테이너는 K8s에 올라갑니다. 즉 설정 오류가 클라우드에 도달하기 전에 파이프라인 안에서 걸러지고 고쳐지는 구조입니다.

발표 데모에서는 일부러 취약한 Terraform 코드를 PR로 올리고, 이 흐름이 자동으로 돌아 PR에 패치와 코멘트가 달리는 모습을 보여주게 됩니다.

**주요 용어·도구**

Terraform은 AWS 같은 클라우드 자원을 코드 파일로 정의하고 명령 한 번으로 생성·변경하는 IaC(Infrastructure as Code) 도구입니다. 우리 프로젝트의 "검사 대상"이 바로 이 Terraform 파일입니다. `plan`은 변경 예정 사항 미리보기, `apply`는 실제 적용입니다.

Trivy는 오픈소스 보안 스캐너입니다. 원래 컨테이너 이미지의 CVE(알려진 소프트웨어 취약점)를 찾는 도구였는데, tfsec를 흡수하면서 `trivy config` 모드로 Terraform·Dockerfile·K8s 매니페스트의 설정 오류도 검사할 수 있게 됐습니다. 우리는 이 하나로 IaC 스캔(1차)과 이미지 스캔(2차)을 모두 처리합니다.

CIS Benchmark는 CIS(Center for Internet Security)가 발행하는 보안 설정 기준서입니다. "루트 계정에 MFA를 켜라", "S3 버킷을 퍼블릭으로 두지 마라" 같은 항목 수백 개가 있고, Trivy가 판단 기준으로 씁니다. "우리 코드가 CIS AWS Benchmark를 얼마나 만족하는가"가 탐지의 정의입니다.

설정 오류(Misconfiguration)는 소프트웨어 자체의 결함(취약점, CVE)과 구별되는 개념으로, 정상적인 기능을 잘못 설정해서 생기는 위험입니다. 공동 책임 모델(Shared Responsibility Model)에 따라 AWS 인프라 자체는 AWS가 책임지고, 그 위의 설정은 사용자 책임이라 우리 점검 범위는 후자입니다.

IAM(Identity and Access Management)은 AWS에서 "누가 무엇을 할 수 있는가"를 정하는 권한 체계입니다. 과도한 권한(예: `*:*`)이 대표적인 설정 오류이며, 파이프라인 자체가 AWS에 접근할 때도 최소 권한 IAM을 써야 합니다.

보안 그룹(Security Group)은 AWS의 인스턴스 단위 가상 방화벽입니다. 어떤 IP에서 어떤 포트로 들어올 수 있는지를 정하며, 0.0.0.0/0 개방이 가장 흔한 실수입니다.

GitHub Actions는 GitHub에 내장된 CI/CD 실행기입니다. PR이 열리거나 커밋이 올라오면 정의된 워크플로(YAML 파일)를 자동으로 실행합니다. 우리 파이프라인 전체가 여기서 돌아갑니다.

CI/CD는 코드 변경을 자동으로 검사·빌드(CI)하고 배포(CD)하는 절차를 뜻합니다. 우리 프로젝트는 CI 단계, 즉 배포 전에 개입한다는 점이 핵심입니다.

AI Agent는 단순히 답을 한 번 내는 LLM 호출이 아니라, 스캔 결과를 읽고 → 코드를 고치고 → 확신도를 판단하고 → 재검증 결과를 보고 필요하면 다시 시도하는, 도구를 쓰며 여러 단계를 스스로 진행하는 프로그램입니다. Claude API 등을 기반으로 팀원 B가 구현합니다.

확신도 게이트(Confidence Gate)는 우리 프로젝트의 고유 개념입니다. Agent가 패치마다 High/Medium/Low 확신도를 매기고, 그 등급에 따라 자동 적용·리뷰 요청·진단만 남김으로 행동을 달리합니다. 확신도는 규칙 유형(정답이 하나인지), 패치 범위(몇 줄 바뀌는지), 재검증 통과 여부 등을 조합해 산정하는 것을 목표로 합니다.

PR(Pull Request)은 코드 변경을 본 저장소에 합쳐달라고 요청하는 단위입니다. 우리 파이프라인의 입력이자 출력이고, 사람과 AI가 만나는 접점입니다.

Docker/컨테이너는 애플리케이션과 실행 환경을 하나의 이미지로 묶은 것이고, K8s(Kubernetes)는 그 컨테이너를 여러 대에 배포·운영하는 시스템입니다. 우리 프로젝트에서는 보조 검사 대상(이미지 CVE)이자 최종 배포처입니다.

이 흐름과 용어집을 README에 "동작 시나리오" 섹션으로 넣어두면 팀원 B, C와 인식을 맞추기에도 좋을 것 같은데, 문서 파일로도 만들어 드릴까요?
