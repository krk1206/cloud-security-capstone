# 동작 흐름 (시나리오)

> 개정 이력
> - 2026-09-08 초안
> - 2026-09-08 개정: 검증 스택 8계층 반영, 시나리오 예시를 S3에서 IAM으로 교체, 자율성 등급과 검증을 별개 축으로 분리, K8s 배포 서술 제거

---

## 1. 운영 경로 — 사람 승인이 있는 기본 흐름

팀원이 AWS에 새 서비스를 올리려고 Terraform 코드를 작성한다. EC2 인스턴스와 보안 그룹, 그리고 EC2가 S3에 접근할 IAM 역할을 정의했는데, 급하게 짜느라 **보안 그룹의 SSH 포트(22번)를 `0.0.0.0/0`으로 열어놓았고, IAM 정책에 `Action: "s3:*"`, `Resource: "*"` 를 그대로 두었다.** 이 코드를 GitHub에 PR로 올린다.

### 1단계 — 탐지

PR이 올라오면 GitHub Actions가 실행된다. `trivy config`가 Terraform 코드를 훑어 CIS AWS Benchmark 기준에 어긋나는 설정을 찾아낸다. 여기서 "SSH 전체 개방(HIGH)"과 "IAM 와일드카드 권한" 두 건이 잡힌다. 결과는 JSON으로 저장된다.

**이 단계에 AI는 관여하지 않는다.** 탐지는 Trivy가 결정론적으로 수행한다.

### 2단계 — 분석과 패치 생성

AI Analyzer가 JSON을 받는다. 탐지 항목마다 문제가 된 Terraform 코드 조각, Trivy 규칙 ID, 설명을 함께 읽고 수정 코드를 작성한다. 보안 그룹은 CIDR을 사내 IP 대역으로 좁히고, IAM은 애플리케이션 코드를 참조해 실제 필요한 액션과 리소스 ARN으로 좁힌다.

각 패치에 **확신도 등급을 제안**한다. 다만 이 제안은 **하향 방향으로만** 반영된다(3단계 참조).

### 3단계 — 검증 스택 (V1~V6)

**여기가 이 프로젝트의 핵심이다.** 패치는 다음을 순서대로 통과해야 한다.

| 계층 | 내용 |
|---|---|
| V1 | Trivy 재스캔 — 지목된 룰이 사라졌는가 |
| V2 | Trivy 전체 스캔 — 새 finding이 늘지 않았는가 (보조 신호) |
| V3 | `terraform fmt` / `validate` — 문법이 유효한가 |
| V4 | `terraform plan` — 계획이 생성되는가 |
| V5 | plan JSON diff — 변경 범위가 화이트리스트 안인가 |
| **V6** | **Intent Oracle — 실효 보안 상태가 실제로 개선됐는가** |

**V6가 왜 필요한가.** V1(재스캔)만으로는 부족하다는 것을 직접 측정했다. Trivy 0.74.0에서 `0.0.0.0/0`을 `0.0.0.0/1` + `128.0.0.0/1`로 쪼개면 스캐너는 통과시키지만 두 CIDR의 합집합은 `0.0.0.0/0`과 동일하다. 즉 **보안 상태는 전혀 개선되지 않았는데 재스캔은 깨끗하다.** 실험 기록은 [`experiments/trivy-sg-probe/`](experiments/trivy-sg-probe/)에 있다.

V6는 Trivy 결과를 입력으로 쓰지 않고, plan JSON에서 해당 포트로 들어오는 모든 출처(`cidr_blocks`, `ipv6_cidr_blocks`, prefix list 내용, 참조된 SG)를 모아 **CIDR 합집합을 계산**한다. 문자열 검사가 아니라 산술 계산이어야 한다.

**검증에 실패하면 여기서 멈춘다.** 자율성 등급과 무관하다.

### 4단계 — 자율성 등급 판정

검증을 통과한 패치만 이 단계에 온다. Risk Rubric(변경 리소스 종류, 영향 리소스 수, IAM 관련 여부, blast radius, 롤백 난이도)이 **자율성 등급의 상한**을 정한다.

- 보안 그룹 CIDR 축소 → 단일 리소스, 롤백 쉬움 → 상한 **High**
- IAM 정책 축소 → 권한 변경, 영향 범위 큼 → 상한 **Low**

AI가 제안한 등급이 상한보다 높으면 **강제로 하향**된다. 낮추는 것은 허용된다.

> **검증과 등급은 별개 축이다.** 검증은 "이 패치가 실제로 고쳤는가"를 묻고, 등급은 "잘못됐을 때 파급이 얼마나 큰가"를 묻는다. 등급 산정에 검증 통과 여부를 섞지 않는다.

### 5단계 — PR과 승인

| 등급 | 동작 |
|---|---|
| **High** | 패치 브랜치 + PR 자동 생성. Intent Oracle 판정 요약을 첨부하고 경량 확인 후 병합 |
| **Medium** | PR 생성 + `plan` diff·검증 결과 전체 첨부. 승인자가 읽고 판단 |
| **Low** | 패치를 커밋하지 않고 진단 리포트만 생성. 미패치 사유 기록 |

사람 개발자는 PR 화면에서 "보안 그룹은 검증을 모두 통과했고, IAM은 AI가 제안한 권한 범위가 맞는지 확인해 달라"는 정리를 보게 된다.

### 6단계 — 반영과 사후 검증

승인 후 병합하면 `terraform apply`로 AWS 샌드박스에 반영된다. 그 다음:

| 계층 | 내용 |
|---|---|
| V7 | `describe-security-groups` 등으로 **실제 AWS 상태 실측.** prefix list는 `describe-managed-prefix-lists`로 전개해 확인 |
| V8 | 정상 기능 확인 — 허용돼야 할 트래픽이 실제로 통하는가 |

**V7이 정적 검사와 다른 점**은 배포된 실물을 본다는 것이다. TerraProbe 연구는 `apply`를 수행하지 않아 plan JSON이 최강 증거였으나, 우리는 샌드박스가 있으므로 여기까지 확인한다.

---

## 2. 실험 경로 — 자동 처리 시간 측정용

운영 경로는 사람 승인을 거치므로 "순수 자동 처리 시간"을 잴 수 없다. 이를 측정하기 위해 **샌드박스 한정으로** 무인 경로를 별도 실행한다.

```
검증 스택 V1~V6 통과 → 자동 병합 → 자동 apply
  → V6/V7 실패 시 자동 revert
  → 사람 개입 없는 처리 시간 기록
```

**revert 트리거가 V6/V7이라는 점이 중요하다.** 재스캔과 헬스체크만으로는 01-cidr-split 같은 케이스를 잡지 못한다(재스캔 0 FAIL, 헬스체크 정상 통과, 그러나 전 인터넷 개방). 실험이 이를 확인했기 때문에 트리거를 바꿨다.

**이 경로는 AWS 샌드박스 계정에서만 실행하며, 프로덕션에는 적용하지 않는다.** 실행 사실과 revert 실패 사례를 문서에서 숨기지 않는다.

---

## 3. 발표 데모

일부러 잘못된 Terraform 코드를 PR로 올리고, 파이프라인이 자동으로 돌아 PR에 패치와 검증 결과가 달리는 흐름을 보여준다.

**핵심 장면은 "고쳐지는 것"이 아니라 "걸러지는 것"이다.** Seeded Deceptive Patch(01-cidr-split)를 투입해 Trivy는 통과시키지만 Intent Oracle이 차단하는 장면을 보여주면, 이 프로젝트가 왜 필요한지가 한 화면에 담긴다.

---

## 주요 용어·도구

**Terraform** — AWS 같은 클라우드 자원을 코드 파일로 정의하고 명령으로 생성·변경하는 IaC(Infrastructure as Code) 도구. 우리 프로젝트의 검사 대상이 이 Terraform 파일이다. `plan`은 변경 예정 사항 미리보기, `apply`는 실제 적용.

**Trivy** — 오픈소스 보안 스캐너. 원래 컨테이너 이미지의 CVE를 찾는 도구였는데, tfsec을 흡수하면서 `trivy config` 모드로 Terraform 등의 설정 오류도 검사한다. 룰 ID는 `AVD-AWS-*` 체계. **버전마다 룰셋이 다르므로 실험 시 버전을 기록한다** (현재 0.74.0).

**CIS Benchmark** — CIS(Center for Internet Security)가 발행하는 보안 설정 기준서. Trivy가 판단 기준의 일부로 사용한다. **모든 Trivy 룰이 CIS 항목과 1:1 대응하지는 않으므로, 직접 대응 가능한 항목만 매핑한다.**

**설정 오류(Misconfiguration)** — 소프트웨어 자체의 결함(취약점, CVE)과 구별되는 개념으로, 정상적인 기능을 잘못 설정해서 생기는 위험. 공동 책임 모델(Shared Responsibility Model)에 따라 AWS 인프라 자체는 AWS가 책임지고 그 위의 설정은 사용자 책임이므로, 우리 점검 범위는 후자다.

**IAM** — AWS에서 "누가 무엇을 할 수 있는가"를 정하는 권한 체계. `Action: "*"`, `Resource: "*"` 같은 과도한 권한이 대표적인 설정 오류다. 파이프라인 자체가 AWS에 접근할 때도 최소 권한을 쓰며, GitHub Actions는 액세스 키 대신 OIDC를 사용한다.

**보안 그룹(Security Group)** — AWS의 인스턴스 단위 가상 방화벽. 어떤 IP에서 어떤 포트로 들어올 수 있는지를 정한다. `0.0.0.0/0` 개방이 가장 흔한 실수다.

**GitHub Actions / CI/CD** — GitHub에 내장된 CI/CD 실행기. PR이 열리면 워크플로가 자동 실행된다. 우리 프로젝트는 **배포 전(CI 단계)에 개입한다는 점**이 핵심이다.

**AI Analyzer** — 스캔 결과와 코드 컨텍스트를 읽고 Terraform 패치 후보를 생성하는 LLM 기반 구성요소. **판정 권한은 없다.** 확신도 등급을 제안하지만 상한을 넘길 수 없다. 팀원 B가 구현하며, 프레임워크는 일반 Python 상태 머신을 우선하고 필요할 때만 LangGraph를 검토한다.

**검증 스택(Verification Stack)** — V1~V8의 8계층. 패치가 실제로 보안 상태를 개선했는지 판정한다. **이 프로젝트의 핵심 기여다.**

**Intent Oracle (V6)** — Trivy 결과를 입력으로 쓰지 않고 실효 보안 상태를 독립 계산하는 계층. 보안 그룹의 경우 모든 출처의 CIDR 합집합을 계산해 공인 인터넷을 덮는지 판정한다. Python 표준 라이브러리(`ipaddress`)만 사용한다.

**기만적 패치(Deceptive Fix)** — 스캐너·`validate`·`plan`을 모두 통과하면서 실제 보안 상태는 개선하지 않은 패치. TerraProbe 연구(arXiv:2606.26590)에서 정의됐으며, 실제 코드에서 57.1~71.4% 비율로 관측됐다. 우리도 Trivy에서 2종 확인했다.

**자율성 등급(Autonomy Level)** — Risk Rubric으로 산정하는 High/Medium/Low 등급. **위험도가 낮을수록 High다.** 등급에 따라 PR 자동 생성 여부와 승인 요구 수준이 달라진다. **검증 통과 여부와는 별개 축이며, 검증에 실패하면 등급과 무관하게 차단된다.**

**Risk Rubric** — 자율성 등급의 상한을 정하는 결정론적 기준표. 변경 리소스 종류, 영향 리소스 수, IAM 관련 여부, blast radius, 롤백 난이도로 구성된다. 개발 4주차에 사전 고정하고 이후 결과를 보고 바꾸지 않는다.

**PR(Pull Request)** — 코드 변경을 본 저장소에 합쳐달라고 요청하는 단위. 우리 파이프라인의 입력이자 출력이고, 사람과 시스템이 만나는 접점이다.

**Docker / 컨테이너** — 애플리케이션과 실행 환경을 하나의 이미지로 묶은 것. 우리 프로젝트에서는 **선택 확장**(이미지 CVE 스캔)이며, 핵심 Vertical Slice에는 포함되지 않는다. 시간이 부족하면 제거한다.
